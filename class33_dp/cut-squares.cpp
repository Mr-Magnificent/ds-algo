#include <bits/stdc++.h>
using namespace std;

int CutSquares(int length, int width, vector<vector<int>> &strg)
{
    
}

int main(int argc, char **argv)
{
    int length = 36;
    int width = 30;
    vector<vector<int>> strg(length + 1, vector<int>(width + 1, 0));
    cout << CutSquares(length, width, strg) << endl;
}